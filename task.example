# This is example for task configuration. The file is flat bash script with vars assigments
pid_file=$WORK_DIR'/example.pid'
start='while true; do sleep 1; done & echo $! > '$pid_file
stop='kill `cat '$pid_file'`'
user='root'
group='root'
timeout=10
memory=0
check='exit 0'
